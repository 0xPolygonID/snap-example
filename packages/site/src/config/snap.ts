/**
 * The snap origin to use.
 * Will default to the local hosted snap if no value is provided in environment.
 */
export const defaultSnapOrigin = 'web:https://snaps.polygonid.me';
export const CIRCUITS_FETCH_PATH ='https://snaps.polygonid.me';
